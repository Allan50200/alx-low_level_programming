#include <stdio.h>

/**
 * main - the entry poin
 *
 * Description: this program prints numbers starting 10
 * Return: 0(Success)
 */

int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		printf("%d", num);
	}

	printf("\n");
	return (0);
}
